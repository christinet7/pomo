const timer = {
    pomodoro: 25,
    shortBreak: 5,
    longBreak: 15,
    longBreakInterval: 4
};

let numShortBreaks = 0;

document.getElementById("pomodoroButton").addEventListener("click", startPomo);

function startPomo() {
    alert("HELLO");

}